# 说明

```
代码在subsense的基础上进行修改
尝试过减小subsense的距离阈值，失败
尝试过通过上下界，接近计数的框架（压缩背景模型空间，但接近计数似乎没有什么用），失败
尝试过通过多个随机框架进行前景综合（前景被随机检测为前景的概率要大于背景），失败
尝试过通过平均值帧差法（压缩背景模型空间），失败
尝试过在平均值帧差的基础上进行上下界，接近计算的框架，失败。

问题：
树叶区域的错误检测
船底区域的错误检测
压缩不稳定（噪声值无法维持在0.2附近，一般为0.8，因而船底被误检为背景）

出路：
使用OpticalFlow
稳定地控制噪声值
防止错误更新（效果不错）
```
